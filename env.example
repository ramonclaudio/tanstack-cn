# Copy to .env. Client-exposed vars must be prefixed VITE_ (available via
# import.meta.env). Server-only vars go bare (process.env in server functions).

# Where this app is deployed. Used for canonical, og:url, sitemap.
# Also the single-source-of-truth in src/lib/site.ts, update there too.
VITE_SITE_URL=https://tanstack-cn.vercel.app
